#pragma once

#include <algorithm>
#include <atomic>
#include <iostream>
#include <map>
#include <memory>
#include <mutex>
#include <new>
#include <queue>
#include <regex>
#include <string>
#include <thread>

using str = str;

using std_vi  = std::vector<int>;
using std_vs  = std::vector<str>;
using std_vvi = std::vector<std::vector<int>>;
using std_vvs = std::vector<std::vector<str>>;

using std_mss = std::map<str, str>;
using std_msi = std::map<str, int>;
